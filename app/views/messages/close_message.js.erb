$('#message-main').fadeAndRemove('fast', function() {
  <% if action_type == 'send' %>
    var aType = 'sent';
  <% elsif action_type == 'save' %>
    var aType = 'saved';
  <% else %>
    var aType = 'deleted'
  <% end %>

  var msgTitle = 'Message ' + aType;
  var msgBody = 'Your message has been sucessfully ' + aType + '!';

  notify(msgTitle, msgBody, {
      hPos: 'center',
      icon: '<%= image_path 'img/demo/icon.png' %>',
  });
});

$('#message-panel').html("<%= escape_javascript(render(:partial => "messages/close_panel")) %>");
$('#message-nav').html("<%= escape_javascript(render(:partial => "inbox/navigation")) %>");