$('.task-panel').html("<%= escape_javascript(render(:partial => "show_data")) %>");
jQuery(function() {
  $('#show-data').message('This task was successfully assigned.', {
    classes: ['green-gradient']
  });
});
